// STATUS: NOT_STARTED — Phase 2 frontend
export function GeneralSettingsView({ orgSlug }: { orgSlug: string }) {
	return <div data-org={orgSlug}>General Settings — coming soon</div>;
}

export function MembersSettingsView({ orgSlug }: { orgSlug: string }) {
	return <div data-org={orgSlug}>Members Settings — coming soon</div>;
}

export function RolesSettingsView({ orgSlug }: { orgSlug: string }) {
	return <div data-org={orgSlug}>Roles Settings — coming soon</div>;
}

export function BillingSettingsView({ orgSlug }: { orgSlug: string }) {
	return <div data-org={orgSlug}>Billing Settings — coming soon</div>;
}

export function PipelineSettingsView({ orgSlug }: { orgSlug: string }) {
	return <div data-org={orgSlug}>Pipeline Settings — coming soon</div>;
}

export function AppearanceSettingsView({ orgSlug }: { orgSlug: string }) {
	return <div data-org={orgSlug}>Appearance Settings — coming soon</div>;
}
