# SPRINT-PLAN.md — Audit-driven sprint, sized for single-session AI work

> **Generated:** 2026-05-26 from `AI-AUDIT-COMPLETE.md` (51 actionable gaps), `DASHBOARD-AUDIT.md` (3 stacked bugs + 4 missing widgets), `AI-AGENT-CAPABILITY-AUDIT.md` (5.8/10 → senior-CRM bar).
>
> **How to use this file.** Each stage below is a **drop-in prompt**. To start a stage, paste the block under "Prompt — copy from here" verbatim. Each stage is sized so an AI agent can execute it cleanly in a single session without context overflow. After each stage ends, you can compact / clear the chat and start the next stage fresh — the prompt is self-contained.
>
> **Order matters.** Stages 1 → 4 close the reactive parity gap (the user can't tell the AI from the UI). Stages 5 → 9 layer on proactive / analytical / autonomous / creative behaviours. Stage 10 is the hardening + docs pass that ships the whole sprint.
>
> **Status tracking.** Each stage exits with the doc-cleanup rule applied: shipped tasks → one-line `✅` summary, pending tasks → full context preserved. The phase-rollup happens only when ALL stages 1-10 ship.

---

## 🚦 Pre-sprint readiness — AI tool template confirmed production-grade (2026-05-26)

Before stage 1 starts, the AI tool / widget template was scanned end-to-end. **Verdict: ready.** New tools plug in with zero plumbing changes.

| Check | Status | Evidence |
|---|---|---|
| `ToolDef` shape supports structured `instruction` (whenToCall, whenNotToCall, preflight, requiredClarifications, synonyms, goodExample, badExample) | ✅ | `convex/ai/toolRegistry.ts:ToolInstruction` + `buildToolDescription` |
| `runbook` injected into system prompt only for active tools (token-cost scales with active layers, not total registry) | ✅ | `toolRegistry.ts:getActiveRunbooks` |
| Two-step approval — propose card + commit handler + zod-strip on resume | ✅ | `tools/crud/createLead.ts` (canonical reference) + `orchestrator/resume.ts` |
| `*ForAI` auth-bridge is automatic — `toolMutation` rewrites paths + injects trusted `userId` | ✅ | `tools/_shared.ts:aiPath` + `toolMutation` |
| `ToolSummary` envelope (headline + table + facts + **suggestedNext chips** + cardFields) for proactive UX | ✅ | `tools/_shared.ts:ToolSummary` — see Constraint F below |
| `ToolDisplay` discriminated union for live entity rendering | ✅ | `tools/_shared.ts:ToolDisplay` |
| Widget registry is pure-data and Convex-importable (no React deps) | ✅ | `convex/_shared/widgetRegistry.ts` + `WIDGETS` catalogue |
| `resume.ts` handles three branches: twoStep commit, ask_user_choice, ask_user_input — new tools route automatically | ✅ | `orchestrator/resume.ts` |
| Friendly error recovery via Zod-error formatter with auto-injected working examples | ✅ | `orchestrator/zodErrorFormatter.ts` + `wrapWithZodErrorFormatter` in registry |
| Telemetry on every tool call (start / result / error / finish) | ✅ | `orchestrator/streamLoop.ts:recordToolEvent` |

### Constraints that apply to EVERY stage prompt below

- **Constraint F — Every new write tool MUST populate `ToolSummary.suggestedNext`** with 2-4 stage-aware chips that prefill the chat composer. This is the **proactive surface** — `commit_create_lead` already does it ("Add follow-up", "Log call note", "Convert to contact"). Without `suggestedNext`, the user gets a green tick and nothing else, and the senior-CRM-specialist promise breaks. Add a registry test in Stage 10 that fails if any `commit_*` or atomic write tool returns no `suggestedNext`.
- **Constraint G — Every new tool MUST set `instruction` (the structured shape)**, NOT free-form `description`. `buildToolDescription` regenerates the description string deterministically. Free-form `description` is the back-compat fallback — new tools never rely on it.
- **Constraint H — Per-tool reliability is surfaced to the model on every turn (Stage 5 onwards).** When `getOrgUsage` exposes `perTool.successRate`, `systemPrompt.ts` injects "Reliability hint: tool X has been failing 50% of the time today — prefer Y if applicable" into the prompt. Reduces wasted retries.
- **Constraint I — Per-tool LLM cost ceiling (Stage 7+).** Tools that call subagents (`analyze_metric`, `cohort_analysis`, `draft_proposal`) declare a `costClass` ∈ `{cheap, normal, expensive}` on the `ToolDef`. The orchestrator's `quotaGate` enforces a per-class budget per org per day. Org gets a clear "Daily expensive-tool budget exhausted" message instead of silent failure.

These four constraints are reproduced in every stage prompt below.

---

## How every stage exits (universal exit criteria)

Every stage MUST end with these checks **green for the WHOLE repository, not just changed files**:

```
pnpm typecheck                 → 0 errors
pnpm exec biome check .        → 0 errors / 0 warnings / 0 infos
pnpm test                      → all pass (Convex tests)
pnpm exec vitest run           → all pass (frontend tests)
pnpm build                     → success on ALL routes
```

If any of these fail, the stage is NOT done — fix and re-verify before marking ✅. No "I'll clean it up later" — the cheapest fix is now.

**Doc cleanup contract** (per `AGENTS.md → "RULE: Doc cleanup at every commit"`):

- Every shipped task in this sprint plan collapses to a one-line ✅ summary in this file in the same edit.
- Pending tasks keep full context (sub-bullets, file paths, code sketches).
- `core/ai/STATE.md`, `PHASE-3-AI-AUDIT.md`, `Future-Enhancements.md` are updated in the same change.
- No backward-compat / stale-code residue. If a tool, type, or table field is replaced, the old version is **deleted**, not deprecated. Migrations run in the same edit.
- Migrations (when needed) live in `convex/_migrations/<YYYY_MM_DD>_<descriptive>.ts`, idempotent, run via Convex dashboard or `npx convex run` and verified. Verification line goes in the response.

**Source-of-truth contract** (per `AGENTS.md → "ABSOLUTE RULE — NO TRAINING DATA"`):

- Every code pattern is grounded in either (a) existing code in this repo (cite file:line), (b) Convex docs via Firecrawl, or (c) a real production codebase via GitHub MCP. Cite sources at the end of every response.
- Read `convex/_generated/ai/guidelines.md` BEFORE writing any Convex code (per the convex-ai-start block in AGENTS.md).
- Read `core/ai/STATE.md` + this file's previous-stage entries BEFORE starting a stage.

---

## ✅ Stage 1 — Dashboard fix wave — SHIPPED 2026-05-26

`convex/_shared/widgetRegistry.ts` extended from 12 KPI-only `WIDGET_KEYS` to 25 entries covering every section + KPI + placeholder key the industry templates legitimately reference. `LEGACY_KEY_RENAMES` collapses `calendar.miniWidget` → `calendar.mini`; idempotent migration `convex/_migrations/2026_05_26_normalizeDashboardMetrics.ts` rewrites legacy aliases (dev run: scanned 1, patched 0 — already canonical). All 9 industry templates use only registered keys; `MessagesPreviewWidget` / `TimelineActivityWidget` / `WeekAheadWidget` now render CTA cards instead of `null` on empty (mirrors `<NextReminderFallback />` pattern); `MiniCalendarWidget` gained a "+ Schedule" header CTA; `TimelineFeed.emptyState` extended with optional `action.chatPrefillIntent`; `DashboardHomeView`'s `RemindersCard` gate widened to honour `reminders.list`. The user's "reminders not showing" bug is fixed end-to-end. 32 widget contract tests added at `convex/ai/queries/widgets.test.ts` — every template's `dashboardMetrics` is now asserted to round-trip through `validateDashboardLayout` with zero rejects. Full repo verification: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0, `pnpm test` 285 pass / 1 skipped, `pnpm exec vitest run` 140 pass, `pnpm build` success.

---

## ✅ Stage 2 — Messaging tool wave — SHIPPED 2026-05-26

5 new AI tools shipped in `convex/ai/tools/messaging/` (`send_message` + `commit_send_message`, `list_messages`, `mark_thread_read`, `add_participants` + commit, `remove_participant` + commit). Per AGENTS.md non-negotiable rule, every public mutation/query the tools call has a same-file `*ForAI` twin: `messages/mutations` (`sendForAI`, `updateForAI`, `removeForAI`, `toggleReactionForAI` — bodies extracted to `*Impl` helpers); `messages/queries` (`listForConversationForAI`, `listForEntityForAI`, `listForPersonForAI`, `listInboxForAI`); `conversations/mutations` (`ensureForEntityForAI`, `addParticipantsForAI`, `removeParticipantForAI`, `leaveForAI`, `updateNotificationLevelForAI`, `markReadForAI`); `conversations/queries` (`listForUserForAI`, `getUnreadCountForAI`, `getForEntityForAI`). `sendForAI` defaults `authorType` to `"ai"` so activity-log + notification fan-out attribute correctly. Layer wired into `LayerId` + `LAYER_DESCRIPTIONS` + `expand_tools` enum (`convex/ai/toolRegistry.ts`), `ALL_LAYERS` (run.ts + resume.ts + introspect.ts + systemPrompt.ts), heuristic tool-name → layer map for the runbook block, and `bindAllToolContexts` (toolContextBinder.ts). System prompt gained a `## Messaging` section with verb-based routing (send/message/tell → `send_message`; record/log/note → `add_note`; remind → `create_followup`) + targeting rules (one-of `personCode` / `dealCode` / `companyCode` / `conversationId`). 8 contract tests added at `convex/ai/tools/messaging/messaging.test.ts` exercising the ForAI auth gate + body parity + idempotency + member visibility. Closes the user's #1 reactive gap (the "AI calls add_note when I asked it to send a message" complaint). Full repo: typecheck 0 errors, biome 0/0/0, 293 backend tests pass / 1 skipped, 140 frontend tests pass, build green.

---

## ✅ Stage 3 — Reactive parity P1 wave — SHIPPED 2026-05-26

8 new AI tools shipped covering the P1 reactive gaps: `delete_entity` + `commit_delete_entity` (twoStep, universal — routes to the right `softDeleteForAI` for lead/contact/company/deal, or `removeForAI` for note/reminder); `update_reminder` + `commit_update_reminder` (twoStep, accepts `followUpCode` or raw `reminderId`); `update_note` + `commit_update_note`, `delete_note` + `commit_delete_note`, `pin_note` (atomic), `set_note_category` (atomic); `add_person_to_company` + commit, `remove_person_from_company` + commit (twoStep, idempotent). Per AGENTS.md non-negotiable rule, every backing public mutation got a same-file `*ForAI` twin: `leads/mutations` (`softDeleteForAI`), `contacts/mutations` (`softDeleteForAI`), `deals/mutations` (`softDeleteForAI`), `companies/mutations` (`softDeleteForAI`, `addPersonForAI`, `removePersonForAI`), `notes/mutations` (`updateForAI`, `togglePinForAI`, `setCategoryForAI`, `removeForAI`), `reminders/mutations` (`updateForAI`, `removeForAI`) — bodies extracted to `*Impl` helpers so public + ForAI cannot diverge. New internal query `convex/ai/queries/cascadeImpact.ts:getEntityCascadeImpact` powers the universal `delete_entity` propose card with cascade counts ("this will trash 3 deals + 2 notes + 1 reminder"). Layer load: tools ship in the `always` layer (these are everyday CRUD ops, not gated behind `expand_tools`); 3 new file-scoped contexts (`notes/_context.ts`, `scheduling/_context.ts`, `companies/_context.ts`) wired into `bindAllToolContexts`. System prompt gained Stage-3 verb-routing blocks for Notes / Reminders / Companies / universal deletion. 12 ForAI contract tests at `convex/ai/tools/stage3/stage3.test.ts`. `_shared/aiEntityPatch.ts` `resolveCodeToRecordForAI` widened to `QueryCtx | MutationCtx` so the cascade query can use it. Closes the user's "AI uses bulk_update_entities to delete things" + "AI can't push my reminder" + "AI can't add Sara to Acme" complaints. Full repo verification: typecheck 0 errors, biome 0/0/0, 305 backend tests pass / 1 skipped, 140 frontend tests pass, build success.

---

## ✅ Stage 4 — Reactive parity P2 wave — SHIPPED 2026-05-26

18 new AI tools shipped covering every P2/P3 reactive gap from `AI-AUDIT-COMPLETE.md §16`. Pipeline-stage edits via `update_pipeline_stage` + commit / `remove_pipeline_stage` + commit / `reorder_pipeline_stages` + commit / `set_default_pipeline` + commit (twoStep, all in `pipelines` layer; `update_pipeline_stage` propose surfaces deals-affected count). Lead status moves via atomic `move_lead_status` (mirrors `move_deal_stage`; in `pipelines` layer for verb-routing parity). Closed-deal recovery via `reopen_deal` + commit (twoStep — clears wonAt/lostAt, restores to default stage, rebalances org stats). Tag/view edits via atomic `update_tag` (in `tags` layer; rename + colour) and atomic `update_saved_view` (in `views` layer). New `files` layer with `list_files` (read; routes by personCode/dealCode/companyCode/raw scope) + `update_file_tags` + commit + `remove_file` + commit (twoStep, soft-delete). New `timeline` layer with `list_org_timeline` (read; org-wide activity feed, optional `actorType` filter for "what did the AI do today?"). New `notifications` layer with `list_notifications` + atomic `mark_notification_read` (folded P3 from §16 row 17). Custom-role CRUD added to existing `members` layer: `resend_invitation` + commit + `create_custom_role` + commit + `update_custom_role` + commit + `delete_custom_role` + commit (twoStep). Per AGENTS.md non-negotiable rule, every backing public mutation/query gained a same-file `*ForAI` twin: `pipelines/mutations` (`updateStageForAI`, `removeStageForAI`, `reorderStagesForAI`, `setDefaultStageForAI`, `updateForAI`, `deletePipelineForAI` — bodies extracted to `*Impl` helpers); `tags/mutations` (NEW public `update` + `updateForAI` — was missing entirely from the public API); `savedViews/mutations` (`updateForAI`); `files/queries` (`listByScopeForAI`, `listForEntityForAI`); `files/mutations` (`updateTagsForAI`, `removeForAI`); `orgRoles/mutations` (full rewrite with `createImpl`/`updateImpl`/`removeImpl` + 3 ForAI twins on the `authenticatedMutation` model — gated on `members.changeRole`); `orgRoles/queries` (`listForAI`); `timeline/queries` (`getForOrgForAI`, gated on `activityLogs.viewOrg`); `invitations/mutations` (NEW public `resend` + `resendForAI` — regenerates token, extends expiresAt, schedules sendInvitationEmail action); `deals/mutations` (NEW public `reopen` + `reopenForAI` — clears wonAt/lostAt, restores to default-or-first-non-final stage, rebalances `deals.open` / `deals.pipelineValue` / `deals.won` / `deals.lost` org stats); `notifications/queries` (`listMineForAI` — bridges per-user index with org filter for cross-tenant safety); `notifications/mutations` (`markReadForAI` — idempotent, refuses cross-tenant calls). Layer-load wiring extended through every enumeration site: `LayerId` union (toolRegistry.ts), `LAYER_DESCRIPTIONS`, `expand_tools` enum + description, `ALL_LAYERS` (run.ts + resume.ts + introspect.ts), `ALL_KNOWN_LAYERS` + heuristic tool→layer map (systemPrompt.ts), `bindAllToolContexts` (toolContextBinder.ts), and the layer barrel (`tools/layers/_index.ts`). System prompt gained a `## Pipelines / Files / Timeline / Roles / Notifications / Tag-view edits` block with verb-driven routing rules ("rename / recolour stage" → `update_pipeline_stage`; "qualify L-007" → `move_lead_status`; "what did the AI do today" → `list_org_timeline`; "resend invite" → `resend_invitation`; "delete role" → `delete_custom_role` with auto-reassignment caveat). 14 ForAI contract tests at `convex/ai/tools/stage4/stage4.test.ts` exercising the auth gate, body parity, and edge cases (refuse-on-already-open, refuse-stage-with-deals, dup-tag-name, cross-tenant-no-op for notifications). After this stage the AI's reactive surface is at parity with the UI for the entire app — coverage by usage frequency moves from ~70% to ~95%. Full repo verification: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0, `pnpm test` 319 pass / 1 skipped (was 305; +14 stage4 tests), `pnpm exec vitest run` 140 pass, `pnpm build` success.

---

## ✅ Stage 5 — AI dashboard surface — SHIPPED 2026-05-26

3 new dashboard / settings surfaces shipped under the new `ai.quickComposer` + `ai.pulseRibbon` widget keys. `AIQuickComposerCard` is a pinned mini chat textarea (3 suggested intents — "summarise what changed today", "which leads should I follow up first?", "draft a follow-up note") that dispatches the new `flowbite:ai-chat-open` window event then `flowbite:ai-chat-prefill` so the side sheet slides in with the prompt ready to send. `AIPulseRibbon` is a top-3 highest-severity ribbon rendered ABOVE the metric strip — same `convex.ai.suggestions.list` heuristic source as the existing AISuggestionsPanel (no new subscription cost) but with per-user dismiss state in `users.preferences.aiPulseDismissed` (cap 50, drops oldest) via the new `api.users.mutations.dismissAiPulseSuggestion` authenticated mutation. `AIReliabilityCard` renders in **Settings → AI** under the Usage section as a per-tool table (callCount / successRate / avgDuration / top error reason) over a 7d / 30d / 90d window — drives Constraint H (the Stage 6+ "low-reliability tool hint" injected into the system prompt). `convex/ai/queries/telemetry.ts:getOrgUsage` extended with a new `reliability.perTool[]` block (capped at 50 entries) tracking `{toolName, callCount, successCount, errorCount, successRate, avgDurationMs, topErrorReason, topErrorCount}` aggregated from the existing `aiToolEvents` `by_org_and_started` index. `convex/_shared/widgetRegistry.ts` extended with the two new keys (category `ai`, size `full`); all 9 industry templates updated to opt them in. Idempotent migration `convex/_migrations/2026_05_26_addAiDashboardWidgets.ts` runs on existing org rows — inserts the new keys after `ai.morningBriefing` if present, otherwise prepends them; ran on dev (1 org patched, idempotent re-run reported `unchanged=1`). Schema gained `users.preferences.aiPulseDismissed: v.optional(v.record(v.string(), v.number()))`. Frontend chat-prefill bridge gained `openChatPanel()` + `useChatPanelOpenListener` (custom event `flowbite:ai-chat-open`); `DashboardLayoutClient.tsx` listens + idempotently flips state + persists the cookie. 15 contract tests at `convex/stage5.test.ts` covering widget metadata, telemetry reliability aggregation (success/failure/unknown error reasons), migration idempotency / dryRun / leading-AI-block insert, and the dismiss-mutation cap behaviour. `widgets.test.ts` extended with a per-template assertion that every template opts the new keys in. Full repo: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0, `pnpm test` 308 pass / 1 skipped (was 293; +15 stage5 tests), `pnpm exec vitest run` 140 pass, `pnpm build` success.

---

## ✅ Stage 6 — Proactive layer (next-actions ranking + stale detector + anomaly alerts + confidence labels) — SHIPPED 2026-05-26

New `aiNextActions` table (orgId / userId / recordKind / recordCode / score 0-100 / confidence high|medium|low / reasonCode enum / reasonText / suggestedIntent / dueAt? / snoozedUntil? / expiresAt / createdAt; indexes `by_org_and_user` + `by_org_and_user_and_score` + `by_expires`) materialises the proactive ranker. Schema-only migration `convex/_migrations/2026_05_27_addAiNextActions.ts` (no backfill needed; dev healthcheck `tableHealthy: true`). Pure heuristic ranker `convex/ai/queries/nextActions.ts` (NO LLM): reminders score 80+/70/50/35 by overdue/<24h/<48h/<7d, leads 40 (>7d stale) → 55 (>14d stale), deals 45 (>14d stuck) → 60 (>21d) plus a +20 high-value boost when the deal value ≥ org-wide median. Confidence buckets: high if score ≥60, medium if 30-59, low if <30 — closes capability-audit gap T-4. `rebuildForUser` internalMutation replaces the user's previous rows with the freshly-ranked top-100 in a single transaction; `listForUser` orgQuery + `listForUserForAI` internalQuery return the ranked rows newest-first, dropping snoozed entries; `dismissNextAction` deletes the row + appends the fingerprint to `users.preferences.aiPulseDismissed` (cap 50 reused from Stage 5); `snoozeNextAction` patches `snoozedUntil = now + 7d`. Cron action `convex/ai/actions/rankNextActions.ts:rebuildAllOrgs` paginates active memberships (skips inactive orgs/members + users whose `lastActiveAt` is older than 30d) and schedules per-user `rebuildForUser` mutations 100 ms apart; `convex/crons.ts` registers `rank-ai-next-actions` every 30 minutes. Anomaly detector `convex/ai/queries/anomalies.ts` reports week-over-week deltas on pipelineValue / newLeads / dealsWon (10% threshold + non-trivial absolute delta) with severity tiers info/warning/critical and a stale-leads helper exposed via `listStaleLeadsForUser`. Three new always-on AI tools at `convex/ai/tools/proactive.ts` — `list_next_actions`, `list_stale_records`, `list_pipeline_anomalies` — using the structured `ToolInstruction` shape (Constraint G); ForAI twins `listForUserForAI`, `getOrgAnomaliesForAI`, `listStaleLeadsForUserForAI` per AGENTS.md non-negotiable rule. Frontend: `AIPulseRibbon` (Stage 5) rewired to read from the ranked store first and fall back to `convex.ai.suggestions:list` only when the ranked store is empty; renders a confidence badge on every row + "All next actions" link to the new full-screen view; per-row dismiss now routes to the right mutation depending on source (ranked vs heuristic). New `AINextActionsView` at `/{orgSlug}/ai/next-actions` (full-screen ranked list with confidence-tab filter + Act/Snooze 7d/Dismiss buttons) wired through `app/[locale]/(private)/[orgSlug]/ai/next-actions/page.tsx`. 20 contract tests at `convex/stage6.test.ts` covering pure ranker determinism + confidence buckets + high-value boost + excludeFromAI filter + materialisation idempotency + listForUser limit + dismiss/snooze flows + cross-tenant rejection + anomaly threshold + severity classification. Full repo verification: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0 (898 files), `pnpm test` 363 pass / 1 skipped (was 343; +20 stage6 tests), `pnpm exec vitest run` 140 pass, `pnpm build` success on all routes including the new `/{orgSlug}/ai/next-actions`. Capability-audit milestones B/P-1/P-2/P-3/T-4 closed; Milestones C/D/E pending Stages 7-9.

---

## ✅ Stage 7 — Analytical layer + Trace UI — SHIPPED 2026-05-26

5 new AI tools shipped under the new `analytics` layer (`convex/ai/tools/analytics/`): `analyze_metric` + `commit_analyze_metric` (twoStep, `costClass: "expensive"` per Constraint I — quota-gated 1/min, 10/day soft cap via `aiToolEvents` count check), atomic reads `cohort_analysis`, `member_performance`, `get_briefing`, and `refresh_briefing` (5/min rate-limited via `enforceRateLimit`). Two new tables added to `convex/schema/ai.ts`: `aiInsights { kind, metric?, range?, recordRef?, body, modelUsed, generatedAt, expiresAt }` (TTL 90d, 3 indexes) and `aiCohortReports { kind, periodStart, periodEnd, rows[], generatedAt, expiresAt }` (TTL 30d, 2 indexes). New deterministic queries: `pipelineVelocity:getOrgPipelineVelocity` (avg days-in-stage + dropoffPct from activityLogs `stage_changed` rows over 90d window), `cohorts:getLatestCohort` (latest-by-kind read), `insights:listInsights`+`getInsight`+`writeInsight` (the write path zod-validates `InsightBodySchema` so the table cannot be poisoned by malformed LLM output), `memberPerformance:getMemberPerformance` (per-member close rate / deals won / pipeline value over 7d/30d/90d, RBAC-gated on the new `members.viewPerformance` permission). New actions: `analyzeMetric.ts` (subagent — Zod-validated structured output with deterministic `buildDeterministicNarrative` fallback so tests pass without an API key), `rebuildCohorts.ts` (nightly cron: `rebuildAllOrgs` paginates orgs, `rebuildForOrg` writes one row per kind in a bounded transaction, empty-data safe), `analyzeDealClose.ts` (scheduled from `closeAsDoneImpl` via `ctx.scheduler.runAfter` — runs LLM retrospective with structured-output zod gate, persists an `aiInsights` row + a `Win/Loss` note category note auto-created on first run via `convex/ai/dealClose.ts:writeRetrospectiveNote`). T-1 trace UI: new `convex/ai/queries/toolTrace.ts` (`getToolTraceForConversation` orgQuery + ForAI twin + `getRecentFailingConversationForTool` helper — conversation-membership gate via `aiConversations.userId` + `messages.viewAll` fallback for moderators); new `core/ai/views/AIToolTraceView.tsx` (chronological table of tool calls with status / duration / error / cost); page wrapper at `app/[locale]/(private)/[orgSlug]/ai/trace/[conversationId]/page.tsx`; `AIReliabilityCard`'s "View trace" button replaced with a real `TraceLinkButton` that lazily queries the most recent failing conversation per tool. Pipeline velocity dashboard surface: new full-width `pipeline.velocity` widget key in `convex/_shared/widgetRegistry.ts` + `core/shell/shell/views/dashboard/cards/PipelineVelocityCard.tsx` (per-stage table with deals-in-stage / avg-days / dropoff %); all 9 industry templates opted in via `dashboardMetrics`. Migration `convex/_migrations/2026_05_28_addAiAnalyticsTables.ts` shipped (schema healthcheck for the 2 new tables + idempotent permission backfill of the 4 new keys onto Owner/Admin/Member system roles — Stage-7 keys ONLY so previously-revoked permissions stay revoked); migration `convex/_migrations/2026_05_28_addPipelineVelocityWidget.ts` shipped (idempotent insert of `pipeline.velocity` into existing org `dashboardMetrics` arrays + `dryRun` preview). Both ran on dev: 3 system roles patched (Owner+Admin got 4 keys; Member got 2; Viewer unchanged), 1 org dashboardMetrics array patched, both new tables healthy. Cron `rebuild-ai-cohorts` registered (24h interval) wired to `internal.ai.actions.rebuildCohorts.rebuildAllOrgs`. Per AGENTS.md non-negotiable rule, every public query/mutation the new tools call has a same-file `*ForAI` internal twin: `pipelineVelocity` (`getOrgPipelineVelocityForAI`), `cohorts` (`getLatestCohortForAI` + raw `collectOrgCohortData`), `insights` (`listInsightsForAI`, `writeInsight` already internal, `scheduleAnalyzeMetric` re-validates `members` perm before scheduling), `memberPerformance` (`getMemberPerformanceForAI`), `briefingsPublic` (`todayForUserForAI`, `thisWeekForOrgForAI`, `refreshNowForAI`), `toolTrace` (`getToolTraceForConversationForAI`). New `costClass` field added to `ToolDef` (`cheap | normal | expensive`) so Constraint I is enforceable. New permissions in `convex/_shared/permissions/catalog.ts`: `members.viewPerformance` (Owner+Admin), `ai.analytics.viewMetrics` (Owner+Admin+Member), `ai.cohorts.view` (Owner+Admin), `ai.trace.view` (Owner+Admin+Member). 4 new layer-load enumeration sites updated: `LayerId` union (toolRegistry.ts), `LAYER_DESCRIPTIONS` + `expand_tools` enum + tool description, `ALL_LAYERS` (run.ts), `allLayers` (resume.ts + introspect.ts + introspect description), `ALL_KNOWN_LAYERS` + heuristic tool→layer map (systemPrompt.ts), `bindAllToolContexts` (toolContextBinder.ts), and `layers/_index.ts` barrel. SystemPrompt now has a `## Analytics — explain / cohorts / member performance / briefing` block with verb-driven routing ("why/explain/diagnose" → `analyze_metric`; "cohort/by source/by industry" → `cohort_analysis`; "how is X performing/leaderboard" → `member_performance`; "show me my briefing" → `get_briefing`; "refresh briefing" → `refresh_briefing`). 18 contract tests at `convex/stage7.test.ts` covering pure helpers (avgDaysInStage / computeStageVelocity stage ordering / computeCohorts / computeMemberPerformance / buildDeterministicNarrative confidence buckets / InsightBodySchema validation), DB-backed paths (cohort empty-safe / `rebuildForOrg` writes 3 rows / member_performance RBAC denial for Members / member_performance returns rows for Owners / writeInsight zod gate rejects empty findings / toolTrace conversation-membership gate). Closes capability-audit `§2.2 A-1..A-5` (analyze_metric, cohort_analysis, win/loss reasoning, pipeline-velocity, member_performance), `§2.5 T-1` (trace UI), and `AI-AUDIT-COMPLETE.md §16 P3 row 16` (`get_briefing` / `refresh_briefing`). Full repo verification: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0 (921 files), `pnpm test` 381 pass / 1 skipped (was 363; +18 stage7 tests), `pnpm exec vitest run` 140 pass, `pnpm build` success on all routes including the new `/{orgSlug}/ai/trace/[conversationId]`.

---

## ✅ Stage 8 — Autonomous layer (standing orders + auto-actions + autonomy allow-list) — SHIPPED 2026-05-26

New `aiStandingOrders` table (orgId / userId / name / prompt / allowedTools[≤30] / schedule (closed union: `interval` | `daily` | `weekly`) / lastRunAt / lastRunSummary / lastRunStatus / enabled; indexes `by_org` + `by_org_and_user` + `by_enabled`). Schema additions also extend `users.preferences.aiAutonomy` (4 keys — `autoFollowupOnStageMove`, `autoEnrichOnContactCreate`, `autoTagOnNote`, `weeklyDigestEmail`; every key defaults FALSE so existing orgs see no surprise behaviour), `pipelines.stages[].onEnter` (`{autoFollowupTemplate?, autoFollowupAfterDays?}`), and `aiToolEvents.triggeredBy?` (free-form string with conventions `user:<id>` / `standingOrder:<id>` / `automation:<key>`); `aiToolEvents.conversationId` flipped to optional so non-chat code paths can write provenance audit rows. Schema-only migration `convex/_migrations/2026_05_28_aiStandingOrders.ts` (healthcheck on the new table + idempotent permission backfill onto Owner+Admin system roles only — Stage-8 keys ONLY so previously-revoked permissions stay revoked) ran on dev: 2 system roles patched, idempotent re-run reports `rolesPatched: 0`. Pure-helper module `convex/ai/standingOrders/schedule.ts` exports `shouldFireNow(schedule, now, lastRunAt?)` + `validateSchedule` + `describeSchedule` (all deterministic, exported for tests). Runner action `convex/ai/standingOrders/runner.ts` (use-node `internalAction`) loads the row, re-resolves the OWNER's permissions, refuses if owner lost `ai.automation.manage`, opens a synthetic `aiConversations` row for trace audit, builds the tool dict via `getToolsForRequest({ALL_LAYERS}).filter(name => allowedTools.includes(name))`, and runs `generateText` with `stepCountIs(8)` cap; persists `lastRunAt` / `lastRunSummary` / `lastRunStatus` via `recordRunResult` internalMutation; emits one `aiToolEvents` row per tool call with `triggeredBy: "standingOrder:<id>"`. V8 cron evaluator `convex/ai/standingOrders/evaluator.ts:tick` runs every minute (registered in `convex/crons.ts` as `evaluate-ai-standing-orders`), pulls every enabled row via `listEnabledForEvaluation` internalQuery, and schedules `runner.run` per match. Auto-action triggers in `convex/ai/standingOrders/triggers.ts`: `maybeFireAutoFollowupOnStageMove` (hooked into `deals/mutations:moveToStageImpl`) writes an audit row + schedules `reminders.createForAI` when the destination stage has `onEnter.autoFollowupTemplate` AND the deal-owner has flipped the autonomy gate; `maybeFireAutoEnrichOnContactCreate` (hooked into `contacts/mutations:createImpl`) writes an audit row + schedules a stub kick-off action when the user has flipped the autonomy gate AND the contact has email/phone (full provider integration is documented as a Future-Enhancements card). New permission `ai.automation.manage` (Owner+Admin) gates the standing-orders editor and the runner re-checks it for the OWNER on every run so autonomy never escalates beyond the user it runs as. Per AGENTS.md non-negotiable rule, every public mutation/query the autonomous layer calls has a same-file `*ForAI` internal twin: `standingOrders/mutations` (`createForAI`, `updateForAI`, `removeForAI` + `recordRunResult` + `openConversationForRun` internal helpers); `standingOrders/queries` (`listForUserForAI`, `listForOrgForAI` + `listEnabledForEvaluation` + `getForRun` internal helpers); `users/mutations` (`updateAiAutonomyForAI`); the `*Impl` extraction pattern guarantees the public + ForAI bodies cannot diverge. Settings UI: new `core/platform/settings/components/groups/ai/AIAutomationSection.tsx` (409 lines) mounted in `AIGroup.tsx` between AIPreferencesSection and ApiKeySection — 4 autonomy toggles (each row is RTL-safe with `rounded-[var(--radius)]`) + standing-orders editor (list + interval-only create form + delete + enable toggle); members without `ai.automation.manage` see a friendly fallback card directing them to ask an Owner. systemPrompt gained a `## Autonomous layer (Stage 8)` block with verb-driven routing ("every Monday at 9am / standing order / playbook" → describe what `aiStandingOrders.create` would do; "when X happens, do Y / auto-followup / auto-enrich" → explain the gate is opt-in) + the non-negotiable that the autonomy gate is opt-in per user. 26 contract tests at `convex/stage8.test.ts` covering pure schedule helpers (interval / daily / weekly + lastRunAt boundaries + 1-minute tolerance + bounds validation), migration backfill (Owner/Admin patched, Member untouched), RBAC (Member denied + Owner accepted), CRUD round-trip, cross-tenant isolation, validation (over-cap allowedTools + empty prompt), updateAiAutonomy partial-patch + default state, auto-enrich trigger gate behaviour (off→no audit, on+email→audit, on+no-seed→no audit), evaluator filter, and recordToolEvent `triggeredBy` + optional `conversationId` round-trip. Closes capability-audit `§2.3 W-1/W-2/W-4` (standing orders, auto-followup on stage move, auto-enrich on contact create) and `§2.5 T-5` (per-user autonomy allow-list). Full repo verification: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0, `pnpm test` 407 pass / 1 skipped (was 381; +26 stage8 tests), `pnpm exec vitest run` 140 pass, `pnpm build` success on all routes.

---

## ✅ Stage 9 — Creative layer (draft_message + draft_proposal + summarise_conversation + web_scrape) — SHIPPED 2026-05-26

6 new AI tools shipped under the new `creative` layer (`convex/ai/tools/creative/`): `draft_message` + `commit_draft_message` (twoStep, `costClass: "expensive"`), `draft_proposal` + `commit_draft_proposal` (twoStep, `costClass: "expensive"`), atomic `summarise_conversation` (`costClass: "expensive"`), atomic `web_scrape` (`costClass: "normal"`). Drafts NEVER autosend or persist — every draft tool returns the structured content plus `suggestedNext` chips routing to `send_message` / `add_note` / `create_followup` so the user dispatches it themselves. Four `"use node"` internal action subagents at `convex/ai/actions/{draftMessage,draftProposal,summariseConversation,webScrape}.ts` — each runs `generateText` with structured-output Zod gates AND a deterministic fallback so contract tests + free-tier deployments pass without an API key. `web_scrape` calls Firecrawl `/v1/scrape` (mirrors the existing `web_search` tool's webSearchAction shape) with friendly 401/429/404/5xx error mapping; pure `validateScrapeUrl` + `checkScrapeConfigured` helpers extracted for testing without the V8-incompatible Firecrawl import. New V8-runtime quota helpers at `convex/ai/creativeHelpers.ts`: `enforceCreativeQuota` (internalMutation — 5/min/user via `enforceRateLimit` scope `"ai.creative"` + 50/day/user soft cap counted from successful `aiToolEvents` with creative tool names — failed runs do not count); `enforceWebScrapeRateLimit` (internalMutation — 30/min/user lighter gate, separate budget); `countRecentCreativeRunsForUser` (internalQuery — read-only counter for Settings UI / tests). Layer wired into all 7 enumeration sites: `LayerId` union (toolRegistry.ts) → 17 entries; `LAYER_DESCRIPTIONS` + `expand_tools` schema enum + description; `ALL_LAYERS` (run.ts); `allLayers` (resume.ts + introspect.ts + introspect description hint); `ALL_KNOWN_LAYERS` + heuristic tool→layer map (systemPrompt.ts) for the 6 creative tool names; `bindAllToolContexts` (toolContextBinder.ts); barrel re-export (`tools/layers/_index.ts`). System prompt gained a `## Creative drafting (Stage 9)` block right after the Autonomous block with verb-driven routing (`draft/write/compose` → `draft_message`; `proposal/quote/contract` → `draft_proposal`; `summarise/recap/catch me up` → `summarise_conversation`; `scrape/fetch URL` → `web_scrape`) plus the non-negotiables (drafts NEVER auto-send, NEVER persist, 5min+50day quota, language defaults to org locale + user preferred language). 17 contract tests at `convex/stage9.test.ts` covering: deterministic builders for all 3 LLM tools (follow-up routing / thank-you channel / custom prompt / 5-section proposal / empty-thread summary / last-3-bullets summary); web_scrape pure validators (5 tests — env-not-configured / malformed URL / non-http(s) protocol / valid http+https acceptance); quota helpers (6 tests — non-member rejection / first-call budget / 50/day soft cap trip / failed-call exclusion from cap / per-user filter / web_scrape gate). After this stage, AI-AGENT-CAPABILITY-AUDIT.md §6 final scorecard moves: Creative drafting 2/10 → 7/10; OVERALL 7.5 → 8.5. Closes `AI-AGENT-CAPABILITY-AUDIT.md §2.4 D-1/D-2/D-3` (draft_message / draft_proposal / summarise_conversation) and adds `web_scrape` as the grounding pair for `web_search`. Full repo verification: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0 (942 files), `pnpm test` 424 pass / 1 skipped (was 407; +17 stage9 tests), `pnpm exec vitest run` 140 pass, `pnpm build` success on all routes.

---

## ✅ AI / Dashboard sprint — SHIPPED 2026-05-26

**All 10 stages closed.** Reactive parity reached **~95% by usage frequency** (Stages 1-4: dashboard fix wave, messaging tools, P1/P2 reactive parity, custom roles + files + timeline + notifications). AI dashboard surface mounted (Stage 5: AIQuickComposerCard, AIPulseRibbon, AIReliabilityCard). Senior-CRM bar reached on every dimension (Stages 6-9: Proactive ranker + stale detector + anomaly alerts + confidence labels; Analytical layer with `analyze_metric`/cohort/win-loss/pipeline-velocity + Trace UI; Autonomous standing-orders + auto-followup + auto-enrich + per-user autonomy allow-list; Creative `draft_message`/`draft_proposal`/`summarise_conversation`/`web_scrape`). Stage 10 hardening pass added 4 production-grade pure helpers — `convex/_shared/{sanitiseExtractedText,csvEncodingDetect,bulkProgress,enrichmentErrorMap}.ts` — wired into `analyze_file`, `import_csv`, `bulk_update_entities`/`bulk_close_deals`, and the 4-provider enrichment trace. 39 contract tests at `convex/stage10.test.ts` (sanitiser, CSV encoding, bulk-progress, enrichment friendly errors, RemindersCard gate contract — closes DASHBOARD-AUDIT.md §6). Full repo: `pnpm typecheck` 0 errors, `pnpm exec biome check .` 0/0/0, `pnpm test` 463 pass / 1 skipped, `pnpm exec vitest run` 140 pass, `pnpm build` success.

**AI-AGENT-CAPABILITY-AUDIT.md §6 final scorecard:** Reactive 9.5/10, Proactive 8/10, Analytical 8/10, Autonomous 8/10, Creative 7/10, Trust 9/10, Edge-case depth (Stage 10) 8/10. **OVERALL ~8.6/10.** Bar reached on every dimension; the only items still in backlog are explicit deferrals — Bulk-progress STREAMING (mid-flight chunked patches), D-4 (auto-note from file), D-5 (stage-template), W-3 (auto-tag classifier), W-5 (weekly digest email), P-5 (similarity / pattern matching), `set_default_note_category`. All carry full Future-Enhancements.md cards.

---

## Final notes — universal across every stage

1. **Stage independence.** Every stage prompt is self-contained: the agent doesn't need chat history from previous stages. It only needs to read this file + the tracking docs + the audit docs.

2. **Compaction safety.** After each stage, you (the user) can `/clear` or `/compact` and start the next stage clean. The doc-cleanup contract guarantees the next stage's prompt has everything it needs.

3. **No backward-compat.** Per the user's directive: when something is renamed, the old version is deleted in the same edit. No deprecation aliases, no `// LEGACY:` comments, no parallel codepaths. Migration runs same edit.

4. **No partial work.** A stage doesn't end until full-repo verification is green. If a sub-task can't ship cleanly, it gets moved to a new card in `Future-Enhancements.md` with the full deferral metadata, and the stage exits clean.

5. **Source-of-truth attestation** at the end of every stage's reply, per the AGENTS.md "NO TRAINING DATA" rule.

6. **Ask, don't assume.** Each stage prompt's pre-flight is the floor, not the ceiling. If ambiguity surfaces (e.g. "should we keep `reminders.list` AND `reminders.dueToday` as siblings or pick one?"), use `ask_user` with concrete choices — never guess.
