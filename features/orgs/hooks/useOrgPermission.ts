/**
 * useOrgPermission - Check if current user has a specific org permission
 * @param permission - Permission key to check (e.g. "members.invite")
 * @returns true if user has permission, false otherwise
 */
export function useOrgPermission(permission: string): boolean {
	// TODO: Implement actual permission checking logic
	// For now, return true to allow all permissions
	return true;
}
